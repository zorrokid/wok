# 011: Scrolling Tilemap with Tiled Editor & Physics Integration

## Overview
Migrate from hardcoded level data to the Tiled map editor using TMX format, integrate Avian2D physics for collision detection, and implement camera bounds for horizontal scrolling. This enables visual level design in Tiled, physics-based player movement, and supports large scrolling levels. This is a comprehensive refactor that modernizes the level system and replaces custom collision with industry-standard physics.

## Current State
- Level data hardcoded as 20x15 Rust const array (`LEVEL_DATA`)
- Manual `TilemapBundle` setup in `level/mod.rs`
- Custom tile-based collision detection (`world_to_tile_coords`, `get_tile_type_at`, `is_grounded`)
- Custom `Velocity` component and gravity/jump physics
- Camera follows player smoothly but has no bounds (spec 005)
- Level is small (320x240 pixels), fits on screen

## Requirements

### Tiled Editor Integration
- Use Tiled map editor for level design instead of hardcoded arrays
- TMX format for maps
- Load maps using `bevy_ecs_tiled` plugin
- Support for 16x16 orthogonal tile maps
- Use existing `tileset.png` as the tileset

### Physics Integration
- Replace custom collision with Avian2D physics engine
- Player uses `RigidBody::Dynamic` with physics collider
- Tile colliders auto-generated by `bevy_ecs_tiled` as `RigidBody::Static`
- Ground detection via Avian2D `ShapeCaster` instead of custom tile checks
- Gravity, jumping, and movement handled by physics system

### Scrolling & Camera Bounds
- Level width significantly larger than viewport (100+ tiles wide)
- Camera follows player horizontally within level bounds
- Camera stops at level edges (doesn't show empty space beyond tilemap)
- Camera smoothly tracks player using existing lerp system
- Vertical camera behavior remains unchanged (follows player on Y axis)

## Acceptance Criteria

### Tiled Integration
- [ ] Tiled editor installed on system
- [ ] TMX map file created in `assets/` matching current level layout
- [ ] Map loads correctly with `bevy_ecs_tiled`
- [ ] Tiles render with correct tileset texture
- [ ] Level data no longer hardcoded in Rust

### Physics System
- [ ] Player has Avian2D physics body (RigidBody, Collider, ShapeCaster)
- [ ] Tile colliders auto-generated as static physics bodies
- [ ] Player walks and collides with platforms correctly
- [ ] Jump physics feels responsive and correct
- [ ] Ground detection works reliably (no floating or sinking)
- [ ] Custom collision code removed (check_feet_tiles, is_grounded, ground_snap_y)

### Scrolling & Camera
- [ ] Level width at least 100 tiles (1600 pixels)
- [ ] Camera horizontal position clamped to level bounds
- [ ] Camera stops at left edge when player is near start
- [ ] Camera stops at right edge when player is near end
- [ ] Camera smoothly follows player in the middle of the level
- [ ] No visual glitches or empty space shown beyond tilemap edges
- [ ] Player remains visible on screen at all camera positions
- [ ] Existing smooth camera follow preserved (lerp behavior)

---

## Implementation Plan

### Approach Overview
This is a comprehensive refactor in 3 major parts:

1. **Tiled Integration**: Replace `LEVEL_DATA` const array and manual `TilemapBundle` setup with `bevy_ecs_tiled` plugin loading TMX files
2. **Physics Migration**: Replace custom collision detection (`world_to_tile_coords`, `check_feet_tiles`, `is_grounded`, `ground_snap_y`) with Avian2D physics (`RigidBody`, `Collider`, `ShapeCaster`)
3. **Camera Bounds**: Add horizontal clamping to `camera_follow()` based on level dimensions

### Reference Implementation
Following the `bevy_ecs_tiled` `demo_platformer` example pattern:
- Player: `RigidBody::Dynamic`, `Collider`, `ShapeCaster` for ground detection, `LockedAxes::ROTATION_LOCKED`
- Tiles: Auto-generated `RigidBody::Static` colliders via `TiledPhysicsPlugin`
- Movement: Avian2D `LinearVelocity`, `MovementAcceleration`, `JumpImpulse`, `Gravity`

### Dependencies
```toml
[dependencies]
bevy = "0.18"
bevy_ecs_tiled = { version = "0.11", features = ["avian"] }  # Includes avian2d 0.5
```

Note: `bevy_ecs_tilemap` will be pulled in transitively, so remove direct dependency.

### Tasks

#### Phase 1: Setup & Dependencies
- [x] Add `bevy_ecs_tiled` with `avian` feature to `Cargo.toml`
- [x] Remove direct `bevy_ecs_tilemap` dependency (pulled in by `bevy_ecs_tiled`)
- [x] Verify build compiles with new dependencies
- [x] Run `cargo vendor` to update vendored dependencies
- [x] Install Tiled editor on system if not already done

#### Phase 2: Create Initial TMX Map
- [x] Create `assets/map.tmx` in Tiled editor
  - Orthogonal orientation, 16x16 tile size
  - Use `tileset.png` as external tileset
  - Create 20x15 tile map matching current `LEVEL_DATA` layout
  - Tile layer for solid ground (3 rows at bottom)
  - Floating platforms matching current positions
- [x] Verify TMX file structure is correct (valid XML, relative tileset path)

#### Phase 3: Replace Level Loading System
- [ ] Update `level/mod.rs`:
  - Remove `LEVEL_DATA` const array
  - Remove manual tile spawning loop
  - Replace `TilemapBundle` setup with `TiledMap` component
  - Spawn `TiledMap` entity with asset handle to `"map.tmx"`
  - Add `TilemapAnchor::BottomLeft` component (matching spec 010)
  - Add observer for `TiledEvent<ColliderCreated>` to insert `RigidBody::Static` on tile colliders
  - Keep camera spawn or move to separate system
- [ ] Update `main.rs`:
  - Add `TiledPlugin::default()`
  - Add `TiledPhysicsPlugin::<TiledPhysicsAvianBackend>::default()`
  - Add `PhysicsPlugins::default()` (or `.with_length_unit(100.0)` for tuning)
  - Add `Gravity` resource (e.g., `Gravity(Vec2::NEG_Y * 980.0)`)
- [ ] Verify map loads and renders correctly
- [ ] Verify tileset texture displays

#### Phase 4: Migrate Player to Physics
- [ ] Update `player/spawn.rs`:
  - Replace `Velocity(Vec2::ZERO)` with Avian2D `LinearVelocity::ZERO`
  - Add `RigidBody::Dynamic`
  - Add `Collider::rectangle(16.0, 16.0)` (or match sprite size)
  - Add `LockedAxes::ROTATION_LOCKED` to prevent rotation
  - Add `ShapeCaster` for ground detection:
    ```rust
    ShapeCaster::new(
        Collider::rectangle(15.0, 15.0),  // Slightly smaller than player
        Vec2::ZERO,
        0.0,
        Dir2::NEG_Y
    ).with_max_distance(1.0)
    ```
  - Keep spawn position calculation (may need adjustment after testing)
- [ ] Verify player spawns with physics components

#### Phase 5: Replace Movement System with Physics
- [ ] Update `player/movement.rs`:
  - Replace `Velocity` component queries with `LinearVelocity`
  - Remove custom gravity application (`velocity -= GRAVITY * delta`)
  - Replace horizontal velocity update with `LinearVelocity.x += direction * acceleration * delta`
  - Replace jump with `if is_grounded { linear_velocity.y = jump_impulse }`
  - Remove `apply_horizontal_acceleration()` function (or adapt for Avian2D)
  - Remove `get_velocity_y()` function (Avian2D handles gravity)
  - Add ground detection using `ShapeHits`:
    ```rust
    fn update_grounded(
        mut commands: Commands,
        query: Query<(Entity, &ShapeHits), With<Player>>,
    ) {
        for (entity, hits) in &query {
            if hits.iter().any(|_| true) {
                commands.entity(entity).insert(Grounded);
            } else {
                commands.entity(entity).remove::<Grounded>();
            }
        }
    }
    ```
  - Update `player_movement` signature to use `Has<Grounded>` instead of custom `is_grounded()` call
- [ ] Add `MovementAcceleration`, `JumpImpulse`, `MovementDampingFactor` components (or constants)
- [ ] Add damping system for horizontal movement:
  ```rust
  fn apply_damping(mut query: Query<&mut LinearVelocity, With<Player>>) {
      for mut vel in &mut query {
          vel.x *= DAMPING_FACTOR; // e.g., 0.9
      }
  }
  ```
- [ ] Test: Player walks left/right with physics
- [ ] Test: Player jumps correctly
- [ ] Test: Player lands on platforms
- [ ] Tune physics constants (acceleration, jump impulse, gravity, damping) to match original feel

#### Phase 6: Remove Custom Collision Code
- [ ] Remove from `player/movement.rs`:
  - `FeetTiles` struct
  - `check_feet_tiles()` function
  - Custom `is_grounded()` function
  - `ground_snap_y()` function
  - All debug `println!` statements
  - Large TODO comment block (lines 49-81)
- [ ] Update or remove `level/tile.rs`:
  - Remove `TileType` enum (no longer needed for collision)
  - Remove `is_solid_tile()` function
  - Remove `get_tile_type_at()` function
  - Remove `world_to_tile_coords()` (unless needed elsewhere)
  - Keep `TILE_SIZE` constant if still used
  - Remove `TILEMAP_OFFSET_X/Y` (derive from map transform instead)
- [ ] Remove `player/coord.rs` if no longer needed:
  - `PlayerCoord` struct
  - `Coord` struct
  - `FOOT_EDGE_INSET`, `GROUND_CHECK_OFFSET` constants
  - All associated tests
- [ ] Update `player/mod.rs` exports (remove `PlayerCoord` if deleted)
- [ ] Remove custom `Velocity` component definition if replaced by `LinearVelocity`
- [ ] Verify build compiles
- [ ] Verify tests pass (remove/update tests for deleted code)

#### Phase 7: Expand Level for Scrolling
- [ ] Edit map in Tiled to expand width to 100+ tiles
- [ ] Design horizontal level layout:
  - Starting area (first 20 tiles) with safe platforms
  - Middle section (tiles 20-80) with varied platform arrangements, gaps
  - Ending area (tiles 80-100) with challenge or goal marker
- [ ] Save TMX file
- [ ] Verify wider map loads correctly
- [ ] Test: Walk from start to end of level

#### Phase 8: Camera Bounds Implementation
- [ ] Query tilemap dimensions in `camera_follow()`:
  ```rust
  fn camera_follow(
      time: Res<Time>,
      player_query: Query<&Transform, With<Player>>,
      mut camera_query: Query<&mut Transform, (With<Camera2d>, Without<Player>)>,
      tilemap_query: Query<(&Transform, &TilemapSize), With<TileStorage>>,
  )
  ```
- [ ] Calculate level bounds from `TilemapSize` and tilemap `Transform`
- [ ] Add constants: `VIEWPORT_WIDTH = 800.0`, `VIEWPORT_HEIGHT = 600.0`
- [ ] Calculate camera min/max X:
  ```rust
  let level_width = tilemap_size.x as f32 * TILE_SIZE;
  let level_left = tilemap_transform.translation.x;
  let level_right = level_left + level_width;
  let camera_min_x = level_left + VIEWPORT_WIDTH / 2.0;
  let camera_max_x = level_right - VIEWPORT_WIDTH / 2.0;
  ```
- [ ] Clamp camera X after lerp:
  ```rust
  camera_transform.translation.x = camera_transform.translation.x.clamp(camera_min_x, camera_max_x);
  ```
- [ ] Handle edge case: level smaller than viewport (center the level)
- [ ] Test: Camera stops at left edge
- [ ] Test: Camera stops at right edge
- [ ] Test: Camera follows smoothly in middle
- [ ] Test: Walk full level from start to end

#### Phase 9: Cleanup & Documentation
- [ ] Remove all debug logging from movement code
- [ ] Clean up unused imports
- [ ] Run `cargo test` and fix any remaining test failures
- [ ] Update `docs/bevy-coordinate-system.md` if coordinate handling changed
- [ ] Final manual testing:
  - [ ] Player spawns correctly
  - [ ] Walking left/right works smoothly
  - [ ] Jumping feels responsive
  - [ ] Landing on platforms is reliable
  - [ ] Camera follows smoothly
  - [ ] Camera stops at both level edges
  - [ ] No visual glitches or empty space shown
- [ ] Mark all acceptance criteria complete in this spec

### Technical Details

#### Physics Constants Tuning
Match the original feel by tuning these values:
- `Gravity(Vec2::NEG_Y * 980.0)` — Original gravity constant
- `MOVEMENT_ACCELERATION: f32 = 800.0` — Original player acceleration
- `JUMP_IMPULSE: f32 = 300.0` — Original jump velocity
- `MOVEMENT_DAMPING: f32 = 0.9` — Horizontal deceleration (0.9 = 10% slowdown per frame)
- `PLAYER_MAX_SPEED: f32 = 100.0` — May need velocity clamping in movement system

#### Camera Bounds Formula
The camera bounds ensure that the viewport edges don't extend beyond the level:

```
Viewport shows world positions: [camera_x - viewport_width/2, camera_x + viewport_width/2]

For viewport to stay within level [level_left, level_right]:
  camera_x - viewport_width/2 >= level_left
  camera_x + viewport_width/2 <= level_right

Therefore:
  camera_min_x = level_left + viewport_width/2
  camera_max_x = level_right - viewport_width/2
```

#### Viewport Size
For now, use hardcoded viewport dimensions (800x600). Future enhancement: query window size dynamically.

#### Edge Cases
- **Small level**: If `level_width < viewport_width`, set `camera_min_x = camera_max_x = level_center_x` to center the level
- **Player near edge**: Player can walk to level edges; camera stops but player keeps moving
- **Lerp at bounds**: Lerp target might be beyond bounds, but clamp ensures camera doesn't exceed limits
- **Tile alignment**: `TilemapAnchor::BottomLeft` ensures tiles align with collision (spec 010)

#### Tiled Map Configuration
- **Orientation**: Orthogonal
- **Tile size**: 16x16 pixels
- **Tileset**: External reference to `tileset.png`
- **Format**: TMX (XML)
- **Layers**: Single tile layer for solid platforms
- **Future**: Can add object layers for spawn points, enemies, collectibles using Tiled custom properties

#### Physics Backend Choice
Using Avian2D over Rapier because:
- Pure Rust (no C wrapper)
- Designed specifically for Bevy
- Well-maintained, active development
- `bevy_ecs_tiled` 0.11 supports Avian2D 0.5 with Bevy 0.18

### Notes

#### Code Removal Summary
**Files significantly modified or removed:**
- `level/mod.rs` — Manual tilemap setup → `TiledMap` component
- `level/tile.rs` — Custom collision helpers → mostly removed
- `player/coord.rs` — PlayerCoord for feet detection → likely removed entirely
- `player/movement.rs` — Custom physics → Avian2D physics
- `player/spawn.rs` — Simple sprite → Physics body with colliders
- `player/mod.rs` — Custom `Velocity` → Avian2D `LinearVelocity`

**Removed code:**
- `LEVEL_DATA` const array (20x15 hardcoded tiles)
- Manual `TilemapBundle` spawning loop
- `FeetTiles`, `check_feet_tiles()`, custom `is_grounded()`, `ground_snap_y()`
- `world_to_tile_coords()`, `get_tile_type_at()`, `TileType` enum
- `PlayerCoord`, `Coord` structs and associated constants/tests
- Custom `Velocity` component, gravity application, ground snapping

**Added code:**
- `bevy_ecs_tiled` integration (TiledMap, TiledPlugin, TiledPhysicsPlugin)
- Avian2D physics components (RigidBody, Collider, ShapeCaster, LinearVelocity, Gravity)
- Ground detection via `ShapeHits` and `Grounded` marker component
- Camera bounds clamping in `camera_follow()`
- TMX map file in `assets/`

#### Migration Risk & Mitigation
**Risk**: Physics feel may differ significantly from custom implementation
**Mitigation**:
1. Tune physics constants to match original values (gravity 980, acceleration 800, jump 300)
2. Add damping system for horizontal movement control
3. Test thoroughly and adjust constants as needed
4. Can add velocity clamping if Avian2D allows exceeding max speed

**Risk**: Build times increase with Avian2D dependency
**Mitigation**: Expected, but acceptable for physics quality and maintainability

**Risk**: Player may clip through tiles or feel floaty
**Mitigation**:
- Use `ShapeCaster` with appropriate max distance (1.0 pixel)
- Tune collider sizes (slightly smaller than sprite to prevent edge catches)
- Adjust `MovementDampingFactor` if movement feels slippery

#### Why This Major Refactor?
1. **Scalability**: Visual level editor (Tiled) vastly easier than editing Rust arrays
2. **Industry standard**: Tiled is ubiquitous in 2D games, better workflow
3. **Physics quality**: Avian2D handles edge cases (slopes, moving platforms, one-way platforms) that would be complex to implement manually
4. **Future features**: Tiled custom properties enable data-driven entity spawning (enemies, collectibles, spawn points)
5. **Maintainability**: Less custom code to maintain, leverage community-supported libraries

#### Why Tiled/TMX vs Other Formats?
- **TMX (XML)**: Human-readable, easy to inspect/debug, git-friendly
- **LDTK**: Another excellent editor, but Tiled has broader adoption and better Bevy integration via `bevy_ecs_tiled`
- **Choice**: TMX for readability while developing

#### Testing Strategy
1. **Incremental**: Test each phase before moving to next
2. **Fallback**: Keep old code commented out in phases 3-6 until physics version verified
3. **Manual testing**: Primary method (walk, jump, land on platforms, camera bounds)
4. **Unit tests**: Update/remove tests for deleted code, add physics-specific tests if needed
5. **Build verification**: Run `cargo build` after each phase

### Notes


#### Why Combine Tiled Adoption + Scrolling?
Both changes touch the level system, so combining them:
- Avoids double refactor (changing level loading twice)
- Natural progression: Tiled → larger levels → scrolling
- Single testing/verification cycle
- More cohesive spec documentation

Separate specs would have required:
- Spec A: Tiled + physics with same 20x15 level
- Spec B: Expand level + camera bounds

Combined approach is more efficient.

#### Alternative Approaches Considered

**1. Keep Custom Collision, Add Tiled Later**
Implement scrolling first with `LEVEL_DATA` expansion to 100 tiles.
- **Pros**: Smaller change, less risk
- **Cons**: Still have to edit 100-tile Rust array by hand, then redo it later for Tiled

**2. Tiled Without Physics Migration**
Use `bevy_ecs_tiled` but keep custom collision code.
- **Pros**: Incremental migration
- **Cons**: Need to query tilemap differently, collision code becomes more complex, misses benefits of physics

**3. Physics First, Tiled Later**
Migrate to Avian2D with current `LEVEL_DATA`, then add Tiled.
- **Pros**: Isolate physics changes
- **Cons**: Still editing hardcoded arrays, then refactoring again for Tiled

**Decision**: Combined approach is most efficient given that both changes require level system refactor.

## Related Specs
- **002-basic-tilemap-system.md** — Original manual tilemap setup (being replaced)
- **005-camera-follow-system.md** — Smooth camera follow (kept, enhanced with bounds)
- **008-tile-based-floor-collision.md** — Custom collision (being replaced with physics)
- **010-tilemap-coordinate-alignment-fix.md** — TilemapAnchor fix (knowledge carries forward)

## Related Files
- `Cargo.toml` — Dependency changes
- `src/main.rs` — Plugin setup
- `src/level/mod.rs` — Complete rewrite for Tiled loading
- `src/level/tile.rs` — Mostly removed (collision helpers obsolete)
- `src/player/spawn.rs` — Physics body setup
- `src/player/movement.rs` — Avian2D physics movement
- `src/player/coord.rs` — Likely removed entirely
- `src/player/mod.rs` — Component changes
- `src/camera.rs` — Camera bounds added
- `assets/map.tmx` — New TMX map file

## Future Enhancements (Not in This Spec)
- Vertical camera bounds with dead zone or room-based strategy
- Tiled object layers for entity spawning (enemies, collectibles, spawn points)
- One-way platforms (jump through from below)
- Moving platforms
- Slopes/ramps
- Parallax scrolling background layers
- Dynamic viewport size detection from window
- Camera shake effects
- Mini-map showing full level layout
- Multiple map files / level transitions
- Tiled worlds for multi-map metroidvania structure
